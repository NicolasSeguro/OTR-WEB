.DS_Store
.venv/
__pycache__/
*.pyc
.firecrawl/
.claude/
.cursor/

# Raw Figma export dumps (reference material, not site assets)
images/Pagina Web OTR (*)
images/portfolio-new
images/contact

# Raw video source files (too large for git; compress before adding to site).
# The compressed, web-ready versions live in video/ and ARE tracked.
/video-hero.mp4
/*.mov

# Raw client asset drops (source material, not site assets) — processed
# outputs get copied into images/ and video/ where they belong.
/restantes
/assets
